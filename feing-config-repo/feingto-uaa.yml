spring:
  profiles:
    include: init-db-data-client
  datasource:
#    schema: classpath:/feingto-uaa.sql
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/feingto-uaa?characterEncoding=utf-8
    username: root
    password: root

security.oauth2.resource.id: oauth2-resource
